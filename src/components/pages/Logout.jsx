import React, { useState } from 'react'
import axios from 'axios'

const Logout = () => {
  return (
    <div>
        <button className='btn btn-danger btn-sm px-3'>Logout</button>
    </div>
  )
}

export default Logout